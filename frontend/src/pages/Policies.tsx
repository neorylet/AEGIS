import { FileText as Policy } from 'lucide-react';
import { PlaceholderPage } from '../components/PlaceholderPage';

export const Policies = () => {
  return (
    <PlaceholderPage
      title="Policies"
      description="Configure security policies and rules"
      icon={Policy}
    />
  );
};
