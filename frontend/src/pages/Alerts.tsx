import { AlertTriangle } from 'lucide-react';
import { PlaceholderPage } from '../components/PlaceholderPage';

export const Alerts = () => {
  return (
    <PlaceholderPage
      title="Alerts"
      description="Review and respond to security alerts"
      icon={AlertTriangle}
    />
  );
};
